- name: variable preference
  hosts: local
  connection: local
  # vars: 
  #  Name: " Ansible from play "
  # vars_files:
  #  - course.yaml 
  # vars_prompt:
  #  - name: Name
  #    prompt: Please enter name
  #    private: false
  tasks:
   - name: print Name
    #  vars:
    #   msg: "Ansible from TASK"
     ansible.builtin.debug:
      msg: "Hi {{ Name }} "

      # 1.Arguments-->  has high priority of executing the script 
      # 2.Task
      # 3.file 
      # 4. Prompt
      # 5. play level
      # 6.inventory
      