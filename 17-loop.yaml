- name: packages installation
  hosts: local
  connection: local
  become: true 
  tasks:
   - name: install package
     ansible.builtin.packages: 
      name: "{{item}}"
      state: present
     loop:
     - nginx
     - zip
     - mysql
