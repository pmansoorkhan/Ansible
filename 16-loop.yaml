- name: demo on loop conditions
  hosts: local
  connections: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "Hello,Good Morning {{ item }}" 
    loop:
    - Mansoor khan
    - Mehran khan 
    - Shahina 

       